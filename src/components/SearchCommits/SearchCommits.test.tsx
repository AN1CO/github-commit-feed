import React from 'react';
import { render, screen, waitFor, cleanup } from '@testing-library/react';

describe('Search Commits', () => {
	test('Search button should be disabled on load', () => {});
});
